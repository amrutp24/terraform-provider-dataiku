---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dataiku_project_folder Resource - Dataiku"
subcategory: ""
description: |-
  A folder that projects are organised into on a Dataiku DSS instance.
  Use this with dataiku_project's project_folder_id to place projects into a hierarchy. DSS assigns the id, so reference it as dataiku_project_folder.<name>.id rather than writing one out.
  DSS refuses to delete a folder that still holds projects or child folders, so destroying this resource fails until whatever it contains has moved or gone.
  What a folder contains is deliberately not exposed here. Those are reverse references — projects and child folders point at the folder rather than the other way round — so a resource attribute would always be read before the things that populate it exist. Use the dataiku_project_folder data source to see a folder's contents.
---

# dataiku_project_folder (Resource)

A folder that projects are organised into on a Dataiku DSS instance.

Use this with `dataiku_project`'s `project_folder_id` to place projects into a hierarchy. DSS assigns the id, so reference it as `dataiku_project_folder.<name>.id` rather than writing one out.

DSS refuses to delete a folder that still holds projects or child folders, so destroying this resource fails until whatever it contains has moved or gone.

What a folder contains is deliberately not exposed here. Those are reverse references — projects and child folders point at the folder rather than the other way round — so a resource attribute would always be read before the things that populate it exist. Use the `dataiku_project_folder` data source to see a folder's contents.

## Example Usage

```terraform
resource "dataiku_project_folder" "analytics" {
  name = "Analytics"
}

resource "dataiku_project_folder" "reporting" {
  name      = "Reporting"
  parent_id = dataiku_project_folder.analytics.id
}

# DSS assigns folder ids, so reference the attribute rather than writing one.
resource "dataiku_project" "weekly" {
  project_key       = "WEEKLY"
  name              = "Weekly report"
  owner             = "admin"
  project_folder_id = dataiku_project_folder.reporting.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the folder.

### Optional

- `parent_id` (String) Id of the folder this one sits in. Defaults to `ROOT`, the top of the hierarchy. Changing this moves the folder rather than recreating it.

### Read-Only

- `id` (String) Identifier DSS assigned to the folder.
- `owner` (String) Login that owns the folder. DSS sets this to whoever created it.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Folders are imported by the id DSS assigned them, which you can find with the
# dataiku_project_folder data source starting from ROOT.
terraform import dataiku_project_folder.analytics 1taHPQ6
```
